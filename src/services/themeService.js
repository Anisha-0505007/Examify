const themeKey = 'examify-theme'; export function getTheme() { return localStorage.getItem(themeKey) || 'light'; } export function setTheme(theme) { localStorage.setItem(themeKey, theme); if (theme === 'dark') { document.documentElement.classList.add('dark'); } else { document.documentElement.classList.remove('dark'); } } export function initTheme() { const theme = getTheme(); setTheme(theme); }